{
    "id": "/Election",
    "type": "object",
    "properties": {
        "id": {
            "description": "ID of the election",
            "type": "string"
        },
        "name": {
            "description": "Name of the election",
            "type": "string",
            "minLength": 3,
            "maxLength": 30
        },
        "municipality": {
            "description": "Municipality where the election took place",
            "type": "string",
            "minLength": 3,
            "maxLength": 30
        },
        "date": {
            "description": "Date of the election",
            "type": "string",
            "format": "date"
        },
        "votes": {
            "description": "Total number of votes during the election",
            "type": "integer",
            "minimum": 0
        }
    },
    "required": ["id", "name", "municipality", "date", "votes"]
}

{
    "id": "/Party",
    "type": "object",
    "properties": {
        "id": {
            "description": "ID of the participation",
            "type": "string"
        },
        "partyId": {
            "description": "ID of the party that participated",
            "type": "string"
        },
        "electionId": {
            "description": "ID of the election at which they participated",
            "type": "string"
        },
        "votes": {
            "description": "Number of votes the party received",
            "type": "integer",
            "minimum": 0
        }
    },
    "required": ["id", "partyId", "electionId", "votes"]
}
